import 'package:flutter/material.dart';
import 'package:get/get.dart';

class SecondaryScrool extends StatelessWidget {
  const SecondaryScrool({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("SecondaryScrool", style: TextStyle(color: Colors.white),),
        backgroundColor: Colors.blue,
      ),





    );
  }
}