import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Sahapur extends StatelessWidget {
  const Sahapur({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Sahapur", style: TextStyle(color: Colors.white),),
        backgroundColor: Colors.blue,
      ),





    );
  }
}